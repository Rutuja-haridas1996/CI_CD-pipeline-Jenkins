pipeline {
    agent any

    stages {
        stage('Checkout...') {
            steps {
                echo 'Checkout to Documents folder..'
                sh 'ls -l'
                dir("${env.DOC_WORKSPACE}"){
                    sh "pwd"
                                }

        }
        
        
        
        
        
       
        
        
    }
}
