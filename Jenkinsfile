pipeline {
    agent any

    stages {
        stage('Checkout...') {
            steps {
                echo 'Checkout to Documents folder..'
                 sh 'ls -l'
                dir ('foo') {
                    writeFile file:'dummy', text:''
                }
                sh 'ls -l'
            }
        }
        
        
        
        
        
       
        
        
    }
}
