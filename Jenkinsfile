pipeline {
    agent any

    stages {
        stage('Checkout...') {
            steps {
                echo 'Checkout to Documents folder..'
                sh 'cd /home/rutujaharidas/Documents' 
                sh 'ls -l'
                dir("${DOC_WORKSPACE}/folder") {
                        sh "pwd"
                    }
                sh 'ls -l'
            }
        }
        
        
        
        
        
       
        
        
    }
}
