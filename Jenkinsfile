pipeline {
    agent any

    stages {
        
        stage('Make directory') {
            steps {
                echo 'Make directory..'
               
                sh 'pwd'

            }
        }
        
        
    }
}
