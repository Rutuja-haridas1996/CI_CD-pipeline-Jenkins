pipeline {
    agent any

    stages {
        stage('Checkout...') {
            steps {
                echo 'Checkout to Documents folder..'
                sh 'ls -l'
                echo "${env.DOC_WORKSPACE}"
            }

        }
        
        
        
        
        
       
        
        
    }
}
